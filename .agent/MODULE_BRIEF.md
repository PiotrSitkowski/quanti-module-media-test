# Module Brief — quanti-module-media-test

> Auto-generated by `quanti create` — Phase 6: Domain-Driven AI Inception.
> This file is the AI context anchor for this module. Keep it up to date.

## Domain

**Classification:** generic
**Purpose:** Moduł testowy do zarządzania biblioteką zdjęć w storage object R2
**Requested Integrations:** none

## AI Analysis

### Anti-Bloat Warnings
- No redundancy detected with existing modules.

### Injected Capabilities
- (none)

### Extension Slots
- `media_test_main_view`
- `media_test_detail_panel`
- `dashboard_widget`

### Suggested Schema Fields
- `name` (text, NOT NULL) — Primary display name
- `status` (text, NOT NULL, default: 'DRAFT') — Lifecycle state: 'DRAFT' | 'ACTIVE' | 'ARCHIVED'

### Special Domain Rules
- No special rules applied.

## Testing & Quality (TDD Enforcement)

> **MANDATORY:** Comply with `.agent/rules/TESTING_STANDARDS.md` — violations block deploy.

1. Write tests **BEFORE** implementation — derive test cases from this brief and `definition.ts`.
2. Mock only external calls (D1/R2, fetch). Zod validators and logic must run on real code.
3. Sad-path ratio: **2 error tests per 1 happy-path test** (minimum).
4. `quanti deploy` runs `npm run test` — if tests fail, deploy is blocked.

## UI Development Guidelines

> **HARD RULES** — violations detected by Fleet Guard at deploy time.

1. **React is ALWAYS external.** Do NOT install `react` in `dependencies` — it's in `devDependencies`.
2. **Export all components through `src/components/index.ts`.**
3. **Use `@quanti/ui-kit`** for all UI primitives (Button, Input, Sheet, etc.).
4. **Follow the Design System** (`.agent/rules/UX_UI_STANDARDS.md`): base font `text-[13px]`, no shadows, max `rounded-md`.
5. **Wrap components in `ErrorBoundary` + `Suspense`** in ExtensionSlots.
6. **Context from props** — never import stores directly in slot components.

---
*Regenerate: `quanti create media-test` (without --no-brief)*
